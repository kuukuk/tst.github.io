self.addEventListener('push', function(event) {
  // In diesem Beispiel wird die Benachrichtigung direkt vom clientseitigen JavaScript ausgelöst.
  // Wenn Sie Benachrichtigungen von einem Server senden möchten, würde der Code hier stehen.
});